#![cfg_attr(not(feature = "std"), no_std)]

//! Standalone solo-chain runtime for vibly-chain.

/// Runtime version placeholder used while the solo runtime is wired in later commits.
pub const SOLO_RUNTIME_PLACEHOLDER: &str = "vibly-solo-runtime";
